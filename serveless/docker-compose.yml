version: '2.1'
services:
  localstack:
    image: localstack/localstack
    ports:
      - "4567-4578:4567-4578"
      - "8080:8080"
    environment:
      - SERVICES=sns, sqs, lambda, s3, dynamodb, cloudformation